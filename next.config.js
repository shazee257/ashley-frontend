/** @type {import('next').NextConfig} */
module.exports = {
  reactStrictMode: true,
  // swcMinify: true,
  images: {
    domains: [
      "localhost",
      "ashley-api.herokuapp.com"
    ],
  },
};
